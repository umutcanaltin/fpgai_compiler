# Split grouped ready/valid VHDL

Reference research implementation for `tensor_ports_ready_valid_v1`. One accepted input transaction is duplicated into two output data ports that share one grouped output handshake.
