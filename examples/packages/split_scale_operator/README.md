# SplitScale operator

Maintained FPGAI research operator for validating multi-output external operator import, shape/type inference, numeric reference execution, and implementation selection.

For input `x` it emits `identity=x` and `scaled=scale*x`.
