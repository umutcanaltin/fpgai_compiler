# Identity Ready/Valid VHDL

Maintained research implementation for FPGAI's `scalar_ready_valid_v1` VHDL ABI.
It implements a one-entry elastic register and propagates backpressure through
`input_ready` / `output_ready`.
