# Add tensor_ports_v1 HLS package

Maintained research example for the multi-input `tensor_ports_v1` ABI. It demonstrates an external implementation with two tensor inputs and one tensor output without editing central compiler dispatch.
