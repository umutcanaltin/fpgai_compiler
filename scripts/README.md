# Scripts

This directory is intentionally kept only as a transition notice.

FPGAI public workflows are exposed through:

    python main.py ...
    fpgai ...

Do not add executable workflows here. New reusable functionality should be added
inside the `fpgai/` package and exposed through the CLI when stable.
